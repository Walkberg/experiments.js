export type IsoDate = string; // ISO 8601 date format (e.g., "2023-10-01T12:00:00Z")

export type MemberId = string; // Unique identifier for a member (e.g., user ID or email)

export type OrganizationId = string; // Unique identifier for an organization (e.g., company ID)

export type TemplateId = string; // Unique identifier for a template (e.g., document template ID)

export type UserId = string; // Unique identifier for a user (e.g., user ID or email)
